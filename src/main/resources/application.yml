spring:
  datasource:
    username: root
    password: password
    url: jdbc:mysql://localhost:3306/eversourcedb
    driver-class-name: com.mysql.cj.jdbc.Driver
    thymeleaf:
      prefix: classpath:/templates/
      suffix: .html
      cache: false

server:
  port: 8088


# xmlns:th="http://www.thymeleaf.org"

  